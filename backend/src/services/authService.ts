import jwt from 'jsonwebtoken';
// TODO: generateAuthToken
// TODO: verifyAuthToken
// TODO: decodeAuthToken

const generateAuthToken = (payload: any) => {
  return '';
  
};

const verifyAuthToken = (token: string) => {

}

const decodeAuthToken = (token: string) => {

}

export {
  generateAuthToken,
  verifyAuthToken,
  decodeAuthToken
}

